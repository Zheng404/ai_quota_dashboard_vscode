import { ServiceSettingsDescriptor } from '../types';

export const BRIDGE_SETTINGS: ServiceSettingsDescriptor = {
	keyPlaceholder: '无需手动输入',
	keyHint: 'Cookie Bridge 会自动从浏览器扩展接收凭证',
	showHelpButton: false,
};
